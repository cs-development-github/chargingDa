<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Aide - Partenaires</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  {{ encore_entry_link_tags('landing') }}
  <style> 
    body {
      font-family: 'Segoe UI', sans-serif;
      padding-top: 80px;
      background-color: #f9f9f9;
    }

    .navbar-brand {
      text-decoration: none !important;
    }

    .logo-container img {
      height: 40px;
    }

    .title-gradient {
      font-weight: bold;
      background: linear-gradient(to right, #ec008c, #f6941f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero {
      background: url('{{ asset('build/images/background-homepagz.png') }}') center center / cover no-repeat fixed;
      min-height: 50vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem;
      position: relative;
      color: white;
    }

    .hero::before {
      content: "";
      position: absolute;
      inset: 0;
      z-index: 1;
      background: rgba(255, 255, 255, 0.3);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .hero h1 {
      font-size: 2rem;
      z-index: 2;
      position: relative;
    }

    .hero h5 {
      font-size: 1rem;
      z-index: 2;
      position: relative;
    }

    .section-aide {
      padding: 4rem 1rem;
    }

    .section-aide h2 {
      font-weight: bold;
      background: linear-gradient(to right, #ec008c, #f6941f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .card-custom {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
      transition: transform 0.3s ease;
    }

    .card-custom:hover {
      transform: translateY(-5px);
    }

    .contact-card {
      border-radius: 1.5rem;
      transition: all 0.3s ease;
    }

    .contact-card:hover {
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
    }

    .form-floating {
      position: relative;
    }

    .form-floating .form-control {
      padding-left: 3rem;
    }

    .form-floating label {
      left: 3rem;
    }

    .form-floating .bi {
      position: absolute;
      top: 50%;
      left: 1rem;
      transform: translateY(-50%);
      color: #aaa;
      font-size: 1.2rem;
      pointer-events: none;
    }

    textarea.form-control {
      resize: none;
    }

    .btn-send {
      background: linear-gradient(to right, #ec008c, #f6941f);
      color: white;
      border: none;
      border-radius: 2rem;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 5px 15px rgba(236, 0, 140, 0.2);
    }

    .btn-send:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(236, 0, 140, 0.4);
    }

    .gradient-text {
      background: linear-gradient(to right, #ec008c, #f6941f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-family: 'UrbaneRounded', sans-serif;
      font-weight: 600;
    }

    .title-gradient {
      font-family: 'UrbaneRounded', sans-serif;
      font-size: 24px;
      font-weight: 600;
      background: linear-gradient(to right, #ec008c, #f6941f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .logo-container {
      width: 60px;
      height: 60px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .logo-container img {
      max-width: 80%;
      max-height: 80%;
      object-fit: contain;
    }

    .btn-gradient {
      background: linear-gradient(to right, #ec008c, #f6941f);
      border: none;
      color: #fff;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 30px;
      transition: opacity 0.3s ease;
    }

    .btn-gradient:hover {
      opacity: 0.9;
      color: #fff;
    }

    .gradient-line {
      width: 60px;
      height: 4px;
      background: linear-gradient(to right, #ec008c, #f6941f);
      margin: 0 auto;
    }

    .link-column ul {
      list-style: none;
      padding: 0;
    }

    .link-column a {
      text-decoration: none;
      color: #333;
    }

    .logo-container img {
      height: 40px;
    }
  </style>
</head>

<body>
  {% include 'landing_page/_navbar.html.twig' %}

  <header class="hero">
    <div class="container">
      <h1 data-aos="fade-down">Besoin d'aide ?</h1>
      <h5 class="lead mt-3" data-aos="fade-up">Notre équipe est là pour vous accompagner à chaque étape.</h5>
    </div>
  </header>

  <section class="section-aide">
    <div class="container">
      <div class="row g-4 mb-5">
        <div class="col-md-4" data-aos="fade-up">
          <div class="card card-custom p-4 h-100 text-center">
            <i class="bi bi-wifi fs-1 text-warning mb-3"></i>
            <h5 class="fw-bold mb-2">Problème de supervision</h5>
            <p>Vérifiez votre carte SIM, suivez le guide d'activation ou contactez notre support.</p>
          </div>
        </div>
        <div class="col-md-4" data-aos="fade-up" data-aos-delay="100">
          <div class="card card-custom p-4 h-100 text-center">
            <i class="bi bi-plug fs-1 text-danger mb-3"></i>
            <h5 class="fw-bold mb-2">Compatibilité de votre borne</h5>
            <p>Assurez-vous que votre modèle accepte les cartes SIM. Consultez notre guide de compatibilité.</p>
          </div>
        </div>
        <div class="col-md-4" data-aos="fade-up" data-aos-delay="200">
          <div class="card card-custom p-4 h-100 text-center">
            <i class="bi bi-box-seam fs-1 text-success mb-3"></i>
            <h5 class="fw-bold mb-2">Livraison et installation</h5>
            <p>Suivez votre commande et découvrez les étapes d'installation rapides et simples.</p>
          </div>
        </div>
      </div>

      <div class="contact-card p-5 bg-white shadow-lg rounded-4" data-aos="fade-up" data-aos-delay="300">
        <h2 class="mb-5 text-center title-gradient">Nous sommes à votre écoute</h2>
        <div id="alert-container"></div>
        <form class="contact-form" id="help-form">
          <div class="row g-4">
            <div class="col-md-6">
              <div class="form-floating">
                <input type="text" class="form-control" id="name" name="user_name" placeholder="Votre nom" required>
                <label for="name">Votre nom</label>
                <i class="bi bi-person-fill"></i>
              </div>
            </div>
            <!-- Email -->
            <div class="col-md-6">
              <div class="form-floating">
                <input type="email" class="form-control" id="email" name="user_email" placeholder="Votre email"
                  required>
                <label for="email">Email</label>
                <i class="bi bi-envelope-fill"></i>
              </div>
            </div>
            <div class="col-12">
              <div class="form-floating">
                <input type="text" class="form-control" id="subject" name="subject" placeholder="Sujet" required>
                <label for="subject">Sujet</label>
                <i class="bi bi-chat-left-dots-fill"></i>
              </div>
            </div>
            <div class="col-12">
              <div class="form-floating">
                <textarea class="form-control" id="message" name="message" placeholder="Votre message"
                  style="height: 150px" required></textarea>
                <label for="message">Votre message</label>
                <i class="bi bi-pencil-square"></i>
              </div>
            </div>
          </div>
          <div class="text-center mt-4">
            <button type="submit" class="btn btn-send d-inline-flex align-items-center gap-2 px-4 py-2">
              <i class="bi bi-send-fill fs-5"></i>
              <span class="fw-semibold">Envoyer</span>
            </button>
          </div>
        </form>
      </div>
    </div>
  </section>

		{% include 'landing_page/_footer.html.twig' %}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
  <script>
    AOS.init({ once: true, duration: 1000 });
    emailjs.init("TxmeGLDR5n8VzRPJ8");

    document.getElementById("help-form").addEventListener("submit", function (e) {
      e.preventDefault();
      const alertContainer = document.getElementById("alert-container");

      emailjs.sendForm("service_ql3duuq", "template_g8g800o", this)
        .then(function () {
          alertContainer.innerHTML = `
            <div class="alert alert-success alert-dismissible fade show" role="alert">
              Votre message a été envoyé avec succès !
              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>`;
          e.target.reset();
        }, function () {
          alertContainer.innerHTML = `
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
              Une erreur est survenue. Veuillez réessayer plus tard.
              <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>`;
        });
    });
  </script>
</body>

</html>