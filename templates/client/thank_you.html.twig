<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Félicitations</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script
      src="https://kit.fontawesome.com/a076d05399.js"
      crossorigin="anonymous"
    ></script>
    {{ encore_entry_link_tags("app") }}
    <link rel="stylesheet" href="{{ asset('css/app.css') }}" />
    <style>
      body {
        background-color: #f8f9fa;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .container {
        background: white;
        padding: 60px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        max-width: 700px;
        text-align: center;
        margin: 20px;
      }
      #otpSection {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Félicitations !</h1>
      <p>Vous avez terminé de remplir votre dossier.</p>
      <p>Nous vous l'avons envoyé par email.</p>
      <p>Merci d'avoir choisi les services Lodmi !</p>

      <!-- Bouton pour envoyer un SMS -->
      <button id="sendOtpBtn" class="btn btn-primary mt-3">
        <i class="bi bi-envelope-check"></i> Recevoir un SMS
      </button>

      <!-- Section pour entrer le code OTP -->
      <div id="otpSection" class="mt-3">
        <p class="text-success">Un code de vérification a été envoyé par SMS. Veuillez le saisir ci-dessous :</p>
        <input type="text" id="otpInput" class="form-control mb-2" placeholder="Entrez le code OTP" maxlength="6">
        <button id="verifyOtpBtn" class="btn btn-success">
          <i class="bi bi-check-circle"></i> Valider
        </button>
        <p id="otpMessage" class="mt-2 text-danger"></p>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
    <script>
      window.addEventListener('load', function () {
        confetti({
          particleCount: 150,
          spread: 70,
          origin: { y: 0.6 }
        });
      });

      document.getElementById('sendOtpBtn').addEventListener('click', function() {
        fetch('/send-otp', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({ token: '{{ token }}' })
        })
        .then(response => response.json())
        .then(data => {
          if (data.success) {
            document.getElementById('otpSection').style.display = 'block';
            document.getElementById('sendOtpBtn').style.display = 'none';
          } else {
            alert(data.error);
          }
        });
      });

      document.getElementById('verifyOtpBtn').addEventListener('click', function() {
        const otpCode = document.getElementById('otpInput').value;
        fetch('/verify-otp', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({ token: '{{ token }}', otp: otpCode })
        })
        .then(response => response.json())
        .then(data => {
          if (data.success) {
            window.location.href = '/thank-you';
          } else {
            document.getElementById('otpMessage').innerText = data.error;
          }
        });
      });
    </script>
  </body>
</html>