<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon site Symfony{% endblock %}</title>

    {# Lien vers le CSS Webpack Encore #}
    {{ encore_entry_link_tags('app') }}
</head>
<body>

    {# Contenu des pages spécifiques #}
    <main>
        {% block body %}{% endblock %}
    </main>

    {# Lien vers le JS Webpack Encore #}
    {{ encore_entry_script_tags('app') }}
</body>
</html>